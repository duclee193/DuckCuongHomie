{
    "projects": [
      {
        "name": "client",
        "root": "client",
        "buildCommand": "npm run build",
        "outputDirectory": "client/build"
      },
      {
        "name": "server",
        "root": "server",
        "buildCommand": "npm run build",
        "outputDirectory": "server/build"
      }
    ]
  }